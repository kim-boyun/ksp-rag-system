# RAG 시스템 고도화 방안 총정리

이 문서는 **Anthropic Contextual Retrieval**([참고](https://www.anthropic.com/engineering/contextual-retrieval))과 일반적인 RAG 고도화 기법을 종합한 전체 로드맵입니다.

---

## 1. 개요: 개선이 일어나는 구간

```
[질의] → [쿼리 전처리] → [검색] → [리랭킹] → [프롬프트 구성] → [답변 생성] → [후처리]
              ↑                ↑         ↑              ↑              ↑
           고도화 포인트    인덱스 품질   고도화 포인트   프롬프트/컨텍스트  (선택)
```

- **인덱스/검색**: 인제스트·인덱스 빌드 시 맥락 추가(Contextual Retrieval), 청킹·임베딩 개선
- **쿼리 전처리**: 질문 재작성, 쿼리 확장, HyDE
- **검색·전달 보완**: 관련도 임계값, 메타데이터 필터, 계층적 청킹/부모 확장, 컨텍스트 압축, MMR
- **리랭킹**: LLM 리랭킹, 크로스 인코더 리랭킹
- **생성**: 프롬프트 정교화, 컨텍스트 전달 방식
- **평가**: Retrieval 메트릭(Recall@k 등), 답변 Faithfulness·인용 정확도, A/B 비교

---

## 2. 인덱스·검색 품질 (Contextual Retrieval + 기타)

### 2.1 Contextual Retrieval (Anthropic 권장)

**목적**: 청크만으로는 맥락이 부족해 검색이 실패하는 경우를 줄임.

| 기법 | 설명 | 적용 위치 |
|------|------|-----------|
| **Contextual Embeddings** | 각 청크 앞에 "문서 내 맥락 설명(50~100토큰)"을 LLM으로 생성해 붙인 뒤 **임베딩** | 인덱스 빌드 시 (인제스트 또는 index-elastic) |
| **Contextual BM25** | 같은 "맥락 + 청크" 문자열로 **BM25 인덱스**도 생성 | 동일 |

**프롬프트 예시** (Anthropic 스타일):

```
<document> {{문서 전체 또는 해당 섹션}} </document>
<chunk> {{청크 원문}} </chunk>
이 청크를 문서 전체 맥락 안에서 설명하는 짧은 문맥(50~100토큰)만 작성하세요. 검색 품질 향상을 위한 문맥입니다. 문맥만 출력하세요.
```

**저장 구조 제안**:
- `content`: 원문 청크 (답변 생성 시 사용)
- `contextualized_content`: 맥락 + 원문 (검색·임베딩·BM25용)
- 검색은 `contextualized_content`로 수행, 반환 시 `content`를 프롬프트에 넣거나, 맥락+원문을 함께 넣고 "맥락과 본문 구분" 지시 추가

**참고**: [Contextual Retrieval](https://www.anthropic.com/engineering/contextual-retrieval) 실험 기준 검색 실패율 약 35%↓(임베딩), 49%↓(임베딩+BM25), 리랭킹 포함 시 67%↓.

---

### 2.2 청킹 전략

| 항목 | 설명 | 설정/코드 |
|------|------|-----------|
| 크기·오버랩 | 너무 작으면 맥락 손실, 크면 노이즈 증가 | `CHUNK_SIZE`, `CHUNK_OVERLAP` (.env) |
| 경계 | 문단/섹션 단위로 자르면 품질 향상 | `chunkers.py` 등에서 스플리터 옵션 |
| 표/그림 | 지금처럼 별도 청크 유지 권장 | 유지 |

---

### 2.3 임베딩 모델

| 항목 | 설명 |
|------|------|
| 업그레이드 | 한국어/영어 혼합이면 BGE large, multilingual-E5 등 더 큰 다국어 모델 검토 |
| 비고 | 변경 시 **인덱스 재구축** 필요 |

---

## 3. 쿼리 전처리 (검색 전 단계)

| 기법 | 설명 | 난이도 | 비고 |
|------|------|--------|------|
| **쿼리 확장** | 질문을 여러 표현으로 바꿔 각각 검색 후 RRF 병합 | 쉬움 | 이미 있음. `QUERY_EXPANSION_ENABLED`, `QUERY_EXPANSION_NUM_QUERIES` |
| **질문 재작성** | 사용자 질문을 **검색에 유리한 한 문장**으로 한 번만 재작성 후 검색 | 보통 | Retriever 직전 LLM 1회 호출 |
| **HyDE** | 질문에 대한 **가상 답변**을 LLM으로 생성 → 그 문단을 **임베딩**해서 검색 | 보통 | 질문 문자열만으로 검색할 때보다 문서와 의미 유사도 향상 |

---

## 4. 리랭킹

| 기법 | 설명 | 난이도 | 비고 |
|------|------|--------|------|
| **LLM 리랭킹** | 검색 결과를 LLM으로 관련도 점수(0–100) 매겨 재정렬 | 쉬움 | 이미 있음. UI/CLI "LLM 리랭킹 사용" 또는 `--rerank` |
| **크로스 인코더 리랭킹** | (query, document) 쌍을 한 번에 넣어 관련도 점수 산출. 보통 LLM보다 빠르고 안정적 | 보통 | `rerankers/`에 CrossEncoder 기반 모듈 추가 가능 |

**참고**: Contextual Retrieval 글에서는 초기 검색 top-150 → 리랭크 후 top-20 전달 시 실패율 67% 감소. 본 시스템에서는 `TOP_K`로 초기 검색량, `RERANK_TOP_K`로 최종 전달량 조정.

---

## 5. 검색량·설정 튜닝 (즉시 적용 가능)

| 설정 | 설명 | 권장 |
|------|------|------|
| `TOP_K` | 1차 검색으로 가져올 문서 수 | 12 → 15~20 (리랭킹 사용 시) |
| `RERANK_TOP_K` | 리랭킹 후 답변에 넣을 문서 수 | 5~10 (문서 길이·모델 컨텍스트에 맞게) |
| `LLM_MAX_TOKENS` | 답변이 잘리면 | 2048 등으로 상향 |
| `QUERY_EXPANSION_NUM_QUERIES` | 쿼리 확장 개수 | 3~5 |

---

## 5-1. 검색·전달 보완 (추가 추천)

| 기법 | 설명 | 난이도 | 비고 |
|------|------|--------|------|
| **관련도 임계값(Score cutoff)** | 검색/리랭크 점수가 일정 이하인 문서는 LLM에 넘기지 않음. 노이즈 감소 | 쉬움 | `min_score` 파라미터 추가, 경험적으로 0.5~0.6 등 튜닝 |
| **메타데이터 필터링** | 질문/UI에서 "특정 문서만", "특정 content_type만" 지정 시 ES bool query에 filter 추가 | 보통 | doc_id, source_path, content_type 등으로 범위 축소 → 정밀도 향상 |
| **계층적 청킹 / 부모 확장** | 검색은 작은 청크로 하고, 반환 시 **같은 문서의 앞뒤 청크 또는 상위 섹션**을 함께 가져와 컨텍스트로 전달 | 보통 | chunk에 `parent_id` 또는 `section_id` 저장 후 확장 로직 추가. 맥락 보강 |
| **컨텍스트 압축·선택** | 컨텍스트가 컨텍스트 윈도우를 넘을 때, LLM으로 요약하거나 LongLLMLingua 등으로 중요 문장만 선택해 전달 | 보통 | 긴 문서 다수 전달 시 토큰 절약·집중도 향상 |

---

## 5-2. 다양성·품질 (선택)

| 기법 | 설명 | 난이도 | 비고 |
|------|------|--------|------|
| **MMR (Maximal Marginal Relevance)** | 검색 결과를 “질문과 유사”하면서도 “이미 뽑은 문서와는 다소 다른” 순으로 재정렬. 중복·유사 청크 감소 | 보통 | diversity 파라미터로 novelty vs relevance 균형 |
| **다중 벡터** | 청크별로 "요약 벡터" + "원문 벡터" 등 2종 임베딩 저장 후 검색 시 둘 다 사용해 RRF 병합 | 어려움 | 인덱스 스키마·재구축 필요. 소규모에서 먼저 실험 권장 |

---

## 5-3. 평가·모니터링 (고도화 효과 측정)

| 항목 | 설명 | 비고 |
|------|------|------|
| **Retrieval 메트릭** | Recall@k, MRR, NDCG 등. "질문–정답 구간"을 정답으로 두고 검색 결과에 포함되는지 측정 | 샘플 Q&A 세트 + 정답 doc/chunk 수동 라벨링 후 스크립트로 계산 |
| **답변 품질** | Faithfulness(문서 기반 여부), 인용 정확도(문서 번호가 실제 근거와 일치하는지) | LLM-as-judge 또는 소량 수동 평가 |
| **A/B 비교** | 설정/모델 변경 전후로 동일 질문에 대해 답변·인용 비교 | 우선순위 적용 시 "개선 여부" 판단용 |

---

## 6. 프롬프트·생성 단계

| 항목 | 설명 |
|------|------|
| **qa.txt 정교화** | "출처만 참고", "모르면 모른다고", "사고 과정 출력 금지" 등 규칙 명확화 → 환각·형식 개선 |
| **컨텍스트 전달** | Contextual Retrieval 적용 시, 답변에는 원문(`content`)만 넣거나, 맥락+원문을 넣고 "맥락과 본문을 구분해 참고하라" 한 줄 추가 (Anthropic 권장) |

---

## 7. 적용 우선순위 제안

| 순서 | 구간 | 방안 | 난이도 | 비고 |
|------|------|------|--------|------|
| 1 | 설정 | LLM 리랭킹 켜기, TOP_K/RERANK_TOP_K/쿼리 확장 개수 튜닝 | 쉬움 | 즉시 |
| 2 | 검색·전달 | 관련도 임계값(min_score) 적용, 필요 시 메타데이터 필터링 | 쉬움 | 노이즈 감소, 정밀도 향상 |
| 3 | 인덱스 | **Contextual Retrieval** (맥락 생성 후 임베딩·BM25에 사용) | 보통 | 인덱스 재구축, 대량 청크 시 비용·시간 고려 |
| 4 | 쿼리 | 질문 재작성 또는 HyDE 도입 | 보통 | Retriever 직전 단계 |
| 5 | 리랭킹 | 크로스 인코더 리랭커 추가 (선택) | 보통 | LLM 리랭킹 대체 또는 1차 후보 축소용 |
| 6 | 검색·전달 | 계층적 청킹/부모 확장 또는 컨텍스트 압축 | 보통 | 맥락 보강 또는 토큰 절약 |
| 7 | 청킹·임베딩 | 청크 경계·크기 실험, 임베딩 모델 업그레이드 | 보통 | 인덱스 재구축 |
| 8 | 생성 | 프롬프트·컨텍스트 전달 방식 정리 | 쉬움 | qa.txt, 컨텍스트 필드 선택 |
| 9 | 선택 | MMR, 다중 벡터, 메타데이터 필터 고도화 | 보통~어려움 | 여유 있을 때 |
| 10 | 운영 | 평가 파이프라인(Recall@k, Faithfulness 등) 도입 | 보통 | 고도화 효과 측정·A/B 비교용 |

---

## 8. 참고 자료

- [Contextual Retrieval in AI Systems (Anthropic)](https://www.anthropic.com/engineering/contextual-retrieval)  
- 본 레포: `src/ragapp/pipeline/rag_pipeline.py`, `src/ragapp/prompts/qa.txt`, `src/ragapp/ingest/`, `src/ragapp/index/`

---

*마지막 업데이트: 2025년 기준 정리. 고도화 추천 항목(5-1~5-3, 우선순위 2·6·9·10) 추가.*
